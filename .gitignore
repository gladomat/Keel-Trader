# build artifacts
*.o
*.so
*.dylib
__pycache__/
*.pyc
.venv/
build/

# data (packed market binaries are large; regenerate via sim/export_data.py)
*.bin
data/

# local state (account locks, buy memory) — never commit
strategy_state/
*.lock

# checkpoints / leaderboards / forecast cache / verdicts are run outputs
checkpoints/
*_leaderboard.csv
forecast/cache/
artifacts/

# deploy run output (append-only history written by ops/deploy_live_trader.sh)
ops/deploy_history.log
